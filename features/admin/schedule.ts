export function localDateTimeToIso(value: string, timeZoneOffsetMinutes = new Date().getTimezoneOffset()): string | null { if (!value) return null; const parsed = /^([0-9]{4})-([0-9]{2})-([0-9]{2})T([0-9]{2}):([0-9]{2})$/.exec(value); if (!parsed) return null; const date = new Date(Date.UTC(Number(parsed[1]), Number(parsed[2]) - 1, Number(parsed[3]), Number(parsed[4]), Number(parsed[5])) + timeZoneOffsetMinutes * 60_000); return Number.isNaN(date.getTime()) ? null : date.toISOString(); }
export function isoToLocalDateTime(value: string | Date | null | undefined, timeZoneOffsetMinutes = new Date().getTimezoneOffset()): string { if (!value) return ""; const date = new Date(value); if (Number.isNaN(date.getTime())) return ""; return new Date(date.getTime() - timeZoneOffsetMinutes * 60_000).toISOString().slice(0, 16); }
