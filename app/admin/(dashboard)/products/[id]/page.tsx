import { notFound } from "next/navigation";
import { ExistingContentForm } from "@/components/admin/domain-forms";
import { requireUser } from "@/lib/auth/permissions";
import { prisma } from "@/lib/db/prisma";
import { saveProductAction } from "../actions";

export default async function ProductEditPage({ params }: { params: Promise<{ id: string }> }) { await requireUser(); const { id } = await params; const [product, categories] = await Promise.all([prisma.product.findFirst({ where: { id, deletedAt: null }, include: { translations: true, media: true } }), prisma.productCategory.findMany({ where: { deletedAt: null }, include: { translations: { where: { locale: "en" }, take: 1 } } })]); if (!product) notFound(); const specifications = product.specifications && typeof product.specifications === "object" && !Array.isArray(product.specifications) ? product.specifications as Record<string, unknown> : {}; return <main><ExistingContentForm kind="product" initial={{ id: product.id, version: product.updatedAt.toISOString(), categoryId: product.categoryId, slug: product.slug, casNumber: product.casNumber, sequence: product.sequence, specifications, coverMediaId: typeof specifications.coverMediaId === "string" ? specifications.coverMediaId : product.media[0]?.id, status: product.status, scheduledAt: product.scheduledAt?.toISOString() ?? null, mediaIds: product.media.map((item) => item.id), translations: product.translations.map((item) => ({ locale: item.locale === "zh_CN" ? "zh-CN" : item.locale, title: item.title, body: item.body })) }} categories={categories.map((item) => ({ id: item.id, label: item.translations[0]?.title ?? item.slug }))} save={saveProductAction} /></main>; }
